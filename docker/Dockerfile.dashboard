FROM python:3.12-slim
WORKDIR /app
COPY pyproject.toml ./
RUN pip install --no-cache-dir uv && uv sync --no-dev
COPY apps ./apps
COPY packages ./packages
EXPOSE 8501
CMD ["streamlit", "run", "apps/dashboard/main.py", "--server.port", "8501", "--server.address", "0.0.0.0"]
